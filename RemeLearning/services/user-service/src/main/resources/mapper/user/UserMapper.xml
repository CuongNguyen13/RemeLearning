<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.remelearning.user.mapper.UserMapper">

    <insert id="insert" parameterType="com.remelearning.user.domain.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (user_id, email, password_hash, name, role, created_at, updated_at)
        VALUES (#{userId}, #{email}, #{passwordHash}, #{name}, #{role}, now(), now())
    </insert>

    <select id="findByUserId" resultType="com.remelearning.user.domain.User">
        SELECT id, user_id, email, password_hash, name, role, photo_s3_key, photo_url, created_at, updated_at
        FROM users
        WHERE user_id = #{userId}
    </select>

    <select id="findByEmail" resultType="com.remelearning.user.domain.User">
        SELECT id, user_id, email, password_hash, name, role, photo_s3_key, photo_url, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <update id="updateName">
        UPDATE users
        SET name = #{name}, updated_at = now()
        WHERE user_id = #{userId}
    </update>

    <update id="updatePhoto">
        UPDATE users
        SET photo_s3_key = #{photoS3Key}, photo_url = #{photoUrl}, updated_at = now()
        WHERE user_id = #{userId}
    </update>

</mapper>
