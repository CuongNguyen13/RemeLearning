<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.remelearning.english.practice.mapper.PracticeAttemptMapper">

    <insert id="insert" parameterType="com.remelearning.english.practice.domain.PracticeAttempt">
        INSERT INTO practice_attempts
            (user_id, item_id, category, label, is_correct, attempted_at)
        VALUES
            (#{userId}, #{itemId}, #{category}, #{label}, #{correct}, now())
    </insert>

</mapper>
