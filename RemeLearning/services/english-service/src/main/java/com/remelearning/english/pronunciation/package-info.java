/**
 * Pronunciation domain module: pronunciation scoring and phoneme-level error detection.
 * No controller/service/mapper yet - mirrors the former pronunciation-service skeleton.
 * Follows the same layout as the vocabulary module once implemented: controller/, service/,
 * mapper/, domain/, dto/, event/.
 */
package com.remelearning.english.pronunciation;
