/**
 * Grammar domain module: grammar error detection via LLM analysis of transcripts.
 * No controller/service/mapper yet - mirrors the former grammar-service skeleton.
 * Follows the same layout as the vocabulary module once implemented: controller/, service/,
 * mapper/, domain/, dto/, event/.
 */
package com.remelearning.english.grammar;
