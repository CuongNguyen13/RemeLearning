package com.remelearning.common.content.dictation;

/** One dictation topic (a "chủ đề") - backed by a single folder across all content sources. */
public record DictationTopic(String id, String title) {
}
