package com.remelearning.common.content.dictation;

/** Lightweight listing entry for one lesson (a "bài học") within a {@link DictationTopic} - no audio/script content. */
public record DictationLessonSummary(String id, String title) {
}
